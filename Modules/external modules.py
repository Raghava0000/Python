# external modules :-those modules which we download based on the result requirement and will use them.

# pip:-python package manager

# pip install package(click on cmd)
# pip list or pip freeze--will list the libraries we have downloaded.

# pip unistall package(click on cmd)
# pip uninstall instagramy -r requirements.txt-- wil download the all required modules specified inside the requirements.txt file.
